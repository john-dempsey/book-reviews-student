; Development error reporting — show everything. Laravel's own .env
; APP_DEBUG setting governs what actually reaches the browser; this
; governs what PHP itself reports/logs underneath that.
display_errors = On
error_reporting = E_ALL
