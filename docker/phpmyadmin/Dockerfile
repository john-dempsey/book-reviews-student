# Same image students already know from CC-Y1-WebDev's apache-php-mysql
# template — no changes needed for this project.
FROM phpmyadmin/phpmyadmin

RUN docker-php-ext-install pdo_mysql && docker-php-ext-enable pdo_mysql

EXPOSE 80
